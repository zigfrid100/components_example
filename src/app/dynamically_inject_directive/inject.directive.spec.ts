import { InjectDirective } from './inject.directive';

describe('InjectDirective', () => {
  it('should create an instance', () => {
    const directive = new InjectDirective();
    expect(directive).toBeTruthy();
  });
});
